## A2. rebase, cherry-pick и amend, чтобы пересоздать историю
#### Ќельз€ переписать историю Ч можно создать новую
- `git commit --amend --no-edit` Ч заменить последний коммит ветки на отредактированный с дополнительными изменени€ми без изменени€ сообщени€
- `git rebase <upstream>` Ч применить все коммиты от общего родител€ до текущего к `<upstream>`
- `git rebase -i <upstream>` Ч применить заново все коммиты, указав действие с каждым коммитом
- `git rebase --continue` Ч продолжить rebase после разрешени€ конфликтов
- `git rebase --abort` Ч отменить rabase
- `git cherry-pick <commit>` Ч применить указанный коммит к HEAD