## S2. ’ран€тс€ состо€ни€ директории, постепенна€ сборка коммита
#### ’ран€тс€ файлы, разница вычисл€етс€ на лету
#### Commit index дл€ сборки коммита
1. `git add .` Ч добавить все измененные файлы в индекс
2. `git commit -m <msg>` Ч записать изменени€ из индекса в репозиторий
3. `git status -sb` Ч вывести состо€ние директории и индекса кратко с указанием текущей ветки
4. `git restore .` или `git checkout .` Ч отменить изменени€ в директории по индексу
5. `git restore -S .` или `git reset .` Ч отменить изменени€ индекса по коммиту (отмена `git add .`)
6. `git rm <filename>` Ч удалить файл из индекса, чтобы перестать хранить его историю в репозитории